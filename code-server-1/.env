# Code Server 1 — user1@localhost only
CODE_SERVER_PASSWORD=password

# oauth2-proxy — OIDC client for code-server-1 Authentik app
OAUTH2_PROXY_CLIENT_ID=code-server-1-client-id
OAUTH2_PROXY_CLIENT_SECRET=code-server-1-client-secret

# Generate with: python3 -c 'import secrets; print(secrets.token_hex(16))'
OAUTH2_PROXY_COOKIE_SECRET=d2e7069782f189a437685243789c6a7e

# Only this email is permitted through the proxy (secondary guard)
OAUTH2_PROXY_ALLOWED_EMAIL=user1@localhost
